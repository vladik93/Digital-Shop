import { StreetSearchPipe } from './street-search.pipe';

describe('StreetSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new StreetSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
